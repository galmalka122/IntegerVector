# CMakeList.txt : CMake project for Gal_Malka_week1_tasks, include source 
# project here.
#

target_include_directories (week01_solution PRIVATE ${CMAKE_CURRENT_LIST_DIR})


# TODO: Add tests and install targets if needed.